# Exercício 1

# Faça um algoritmo usando o "for" para mostrar os números pares e impares de 0 a 100.

import math

for imparpar in range(101):
  if (imparpar % 2 == 0):
    parimpar = f"o numero {imparpar} é par"
  else:
    parimpar = f"o numero {imparpar} é impar"
  print({parimpar})